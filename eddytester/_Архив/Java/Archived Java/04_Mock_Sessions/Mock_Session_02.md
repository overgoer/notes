# 🎯 Mock-сессия #2: 1 час

**Дата:** ~20 мая 2026 (по плану: `6_Month_Roadmap_2h_day.md`, стр. 126)  
**Формат:** Интеграционные тесты (TestContainers + REST Assured)  
**Цель:** Проверка практических навыков тестирования реальных зависимостей.

---

## 🧾 Инструкция для кандидата

- Это **интеграционное интервью** — как в Ozon/Тинькофф/Авито.
- Тебе нужно написать **автотест**, а не объяснять теорию.
- Оценка будет по трём критериям: **Syntax Fluency**, **Interview Logic**, **Senior Communication**.
- Время — **60 минут**. Начинай сразу.
- После завершения — отправь файл мне.

---

## 🧩 Часть 1: Кодирование на доске (25 мин)

**Задача:** Напиши тест, который проверяет API `GET /api/v1/products/{id}`:
- При корректном `id` → `200 OK`, в теле — JSON с `name`, `price`, `currency`.
- При несуществующем `id` → `404 Not Found`.
- Используй `TestContainers` для запуска **реального PostgreSQL**, и `REST Assured` для запросов.

**Условия:**
- Допустимо использовать любой фреймворк (JUnit 5 / TestNG), но предпочтительно JUnit 5.
- Обязательно используй `@DynamicPropertySource` для авто-подстановки `jdbcUrl`.
- Используй `@Container static` — PostgreSQL должен быть один на весь класс.
- Не используй `H2` или моки — только реальный контейнер.

**Твой код теста:**
```java
// напиши здесь

```

---

## 📘 Часть 2: Объяснение и тест-дизайн (25 мин)

**Вопрос 1:** Почему важно использовать *реальный PostgreSQL* вместо `H2` или `MockMvc` при тестировании этого API? Приведи **два конкретных примера**, где `H2` даст ложноположительный результат.

> Твой ответ:

**Вопрос 2:** Как ты обеспечишь **чистоту БД** между тестами? (Чтобы тест не оставлял "мусор", влияющий на следующий). Приведи **2 разных способа** и объясни плюсы/минусы каждого.

> Твой ответ:

**Вопрос 3:** Как бы ты автоматически **добавлял тест-данные** (`products`) перед тестом? Не писать SQL в каждом тесте, а использовать готовый механизм.

> Твой ответ:

---

## 🌟 Часть 3: Поведенческий вопрос (10 мин)

**Вопрос:** Расскажи, как ты внедрял TestContainers в legacy-проект, где до этого использовали `H2`. Как ты убеждал команду, какие сложности были и как их решал?

> Твой рассказ (STAR):

---

## 📊 Оценка (только для меня — не заполняй)

| Критерий | Оценка (1–5) | Комментарий |
|----------|--------------|-------------|
| **Syntax Fluency** | [ ] | Правильное использование `@Container`, `@DynamicPropertySource`, `given().when().then()`, `@Test` |
| **Interview Logic** | [ ] | Глубокое понимание различий между `H2` и PostgreSQL, стратегия очистки БД, управление тест-данными |
| **Senior Communication** | [ ] | Чёткое объяснение решений, выбор архитектурных вариантов, умение доносить ценность для команды |

**Итого:** `[ ]/15`

---

## 📌 Чек-лист перед отправкой

- [ ] Код теста написан, с `@Container`, `@DynamicPropertySource`, `given().when().then()`
- [ ] Ответы на вопросы развернутые, с примерами
- [ ] STAR-рассказ полный (Situation, Task, Action, Result)
- [ ] Файл сохранён в `04_Mock_Sessions/`
- [ ] Отправлен мне

---

## 🎁 После завершения

- ✅ Запиши `Мок-сессия #2` в `Progress_Tracking.md`
- ✅ Отметь в `Журнал Обучения.md` — как прошла, что получилось
- ✅ Разреши себе что-то приятное
- ✅ Осознай: интеграционные тесты — это твой козырь перед конкурентами.

---

**P.S.** Если за 1 час не успел — отправь, что сделал. Импровизация и выбор компромиссов — тоже часть Senior-навыка.

**Ты уже на пути. 💡**