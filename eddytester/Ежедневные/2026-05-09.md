---
date: 2026-05-09
day: Saturday
---

# 09 May 2026 — Ежедневные задачи

## 📋 Практикум (до 3 задач)

```dataview
TASK FROM #практикум
WHERE !completed
LIMIT 3
```

## 📝 Посты (до 3 задач)

```dataview
TASK FROM #пост
WHERE !completed
LIMIT 3
```

## ☕ Java (до 3 задач)

```dataview
TASK FROM #java
WHERE !completed
LIMIT 3
```

---

```dataviewjs
// === Прогресс Java ===
const tasks = dv.pages('"eddytester/Java/Бэклог задач.md"')
    .file.tasks
    .filter(t => t.tags.includes("#java") && !t.text.toLowerCase().includes("daily"));

const total = tasks.length;
const done = tasks.filter(t => t.completed).length;
const pct = Math.round(done / total * 100) || 0;
const filled = Math.round(pct / 10);
const bar = '█'.repeat(filled) + '░'.repeat(10 - filled);

dv.span(`**☕ Java:** ${done}/${total} (${pct}%)  ${bar}  → ~сен 2026`);
```

---

## ✏️ Журнал дня

**Что сделал:**
- Создал репозиторий java-learning, поправил Warmup.java с sleepIn
- Arrays.java — сумма массива, new int[0], три имени
- Обновил план обучения до 18 недель

**Что не получилось:**
- Путаюсь с вложением методов в классы — норм, первый день
- Злюсь на элементарные ошибки — перевести в топливо

**На завтра:**
